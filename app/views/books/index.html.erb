<h1>books</h1>
<p>Available as of <%= formatted_time(@available_at) %></p>
<%= page_entries_info @books %>
<ul>
<% @books.each do |book| %>
 <li>
  <%= link_to(book.title, book) %>
</li>

<% end %>
</ulss>
<%= paginate @books %>