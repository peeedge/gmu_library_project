<h1>GMU Library Project</h1>
<p>Available as of <%= formatted_time(@available_at) %></p>
<ul>
	<% @books.each do |book| %>
  <li>
  		<%= link_to(book.title, book) %>
  		by <%= book.author %><br/>
  		
	</li>
<% end %>
</ul>